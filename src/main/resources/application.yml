server:
  port: 80

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql:///travel?serverTimezone=UTC
    username: root
    password: fggzsl11
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

mybatis-plus:
  global-config:
    db-config:
      #????
      id-type: auto
  configuration:
    #?????????????product_time -> productTime?
    map-underscore-to-camel-case: false
    #??log????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

# ????
logging:
  pattern:
    console: '%d{HH:mm:ss.SSS} %clr(%-5level) ---  [%-15thread] %cyan(%-50logger{50}):%msg%n'

mail:
  user: 1294237976@qq.com
  password: wpsykvuyyxdkgecb

project:
  path: http://localhost